// Definicja prostej funkcji
DEF WypiszMax(A, B, C)
BEGIN
    // Testowanie zlozonego wyrazenia z priorytetami:
    IF (A > B) OR (B < C) THEN
    BEGIN
        WRITE("Spelniono warunek poczatkowy!");
    END;
    
    RETURN TRUE;
END

// Główny blok programu
BEGIN
    ARRAY MojaTablica[5];
    x := 10;
    
    WRITE("Podaj wartosc y:");
    READ(y);
    
    // Przypisanie do tablicy
    MojaTablica[0] := x;
    MojaTablica[1] := y;
    
    // Wywolanie funkcji
    wynik := WypiszMax(MojaTablica[0], MojaTablica[1], 100);
    
    // Pętla FOR
    WRITE("Kolejne iteracje:");
    FOR i := 1 TO 3 DO
    BEGIN
        WRITE(i);
    END;
END.
